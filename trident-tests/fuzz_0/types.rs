//! # Trident Generated Types
//!
//! This file is automatically generated by Trident.
//! **DO NOT EDIT THIS FILE MANUALLY**

#![allow(dead_code)]
#![allow(unused_imports)]

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use trident_fuzz::fuzzing::*;

// ============================================================================
// PROGRAM MODULES
// ============================================================================

// ----------------------------------------------------------------------------
// Program: vault
// ----------------------------------------------------------------------------
pub mod vault {
    use super::*;

    // ------------------------------------------------------------------------
    // Program ID
    // ------------------------------------------------------------------------

    /// Returns the program ID for vault
    pub fn program_id() -> Pubkey {
        pubkey!("DLQPS4s2GVmW7iNGHZLWFQsmr4RiEGnxsZQkLXJR8VL4")
    }

    // ------------------------------------------------------------------------
    // Instructions
    // ------------------------------------------------------------------------

    // ....................................................................
    // Instruction: AcceptOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for AcceptOwnershipTransfer
    pub struct AcceptOwnershipTransferInstruction {
        pub accounts: AcceptOwnershipTransferInstructionAccountMetas,
        pub data: AcceptOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AcceptOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct AcceptOwnershipTransferInstructionAccountMetas {
        pub pending_owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for AcceptOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct AcceptOwnershipTransferInstructionAccounts {
        pub pending_owner: Pubkey,

        pub config: Pubkey,
    }

    impl AcceptOwnershipTransferInstructionAccounts {
        pub fn new(pending_owner: Pubkey, config: Pubkey) -> Self {
            Self {
                pending_owner,

                config,
            }
        }
    }

    /// Instruction data for AcceptOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AcceptOwnershipTransferInstructionData {}

    impl AcceptOwnershipTransferInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for AcceptOwnershipTransferInstruction
    impl AcceptOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [30u8, 187u8, 65u8, 5u8, 93u8, 131u8, 38u8, 208u8]
        }

        pub fn data(data: AcceptOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: AcceptOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AcceptOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.pending_owner = AccountMeta::new(accounts.pending_owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.pending_owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CancelOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for CancelOwnershipTransfer
    pub struct CancelOwnershipTransferInstruction {
        pub accounts: CancelOwnershipTransferInstructionAccountMetas,
        pub data: CancelOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CancelOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct CancelOwnershipTransferInstructionAccountMetas {
        pub owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for CancelOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct CancelOwnershipTransferInstructionAccounts {
        pub owner: Pubkey,

        pub config: Pubkey,
    }

    impl CancelOwnershipTransferInstructionAccounts {
        pub fn new(owner: Pubkey, config: Pubkey) -> Self {
            Self { owner, config }
        }
    }

    /// Instruction data for CancelOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CancelOwnershipTransferInstructionData {}

    impl CancelOwnershipTransferInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for CancelOwnershipTransferInstruction
    impl CancelOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [2u8, 184u8, 195u8, 105u8, 138u8, 142u8, 154u8, 75u8]
        }

        pub fn data(data: CancelOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: CancelOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CancelOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Deposit
    // ....................................................................

    /// Main instruction struct for Deposit
    pub struct DepositInstruction {
        pub accounts: DepositInstructionAccountMetas,
        pub data: DepositInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Deposit instruction
    #[derive(Debug, Clone, Default)]
    pub struct DepositInstructionAccountMetas {
        pub user: AccountMeta,

        pub config: AccountMeta,

        pub underlying_mint: AccountMeta,

        pub derivative_mint: AccountMeta,

        pub user_underlying_account: AccountMeta,

        pub user_derivative_account: AccountMeta,

        pub underlying_vault: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for Deposit instruction
    #[derive(Debug, Clone)]
    pub struct DepositInstructionAccounts {
        pub user: Pubkey,

        pub config: Pubkey,

        pub underlying_mint: Pubkey,

        pub derivative_mint: Pubkey,

        pub user_underlying_account: Pubkey,

        pub user_derivative_account: Pubkey,

        pub underlying_vault: Pubkey,
    }

    impl DepositInstructionAccounts {
        pub fn new(
            user: Pubkey,

            config: Pubkey,

            underlying_mint: Pubkey,

            derivative_mint: Pubkey,

            user_underlying_account: Pubkey,

            user_derivative_account: Pubkey,

            underlying_vault: Pubkey,
        ) -> Self {
            Self {
                user,

                config,

                underlying_mint,

                derivative_mint,

                user_underlying_account,

                user_derivative_account,

                underlying_vault,
            }
        }
    }

    /// Instruction data for Deposit
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct DepositInstructionData {
        pub underlying_amount: u64,
    }

    impl DepositInstructionData {
        pub fn new(underlying_amount: u64) -> Self {
            Self { underlying_amount }
        }
    }

    /// Implementation for DepositInstruction
    impl DepositInstruction {
        fn discriminator() -> [u8; 8] {
            [242u8, 35u8, 198u8, 137u8, 82u8, 225u8, 242u8, 182u8]
        }

        pub fn data(data: DepositInstructionData) -> Self {
            Self {
                accounts: DepositInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: DepositInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.underlying_mint =
                AccountMeta::new_readonly(accounts.underlying_mint, false);

            self.accounts.derivative_mint = AccountMeta::new(accounts.derivative_mint, false);

            self.accounts.user_underlying_account =
                AccountMeta::new(accounts.user_underlying_account, false);

            self.accounts.user_derivative_account =
                AccountMeta::new(accounts.user_derivative_account, false);

            self.accounts.underlying_vault = AccountMeta::new(accounts.underlying_vault, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.underlying_mint.clone());

            metas.push(self.accounts.derivative_mint.clone());

            metas.push(self.accounts.user_underlying_account.clone());

            metas.push(self.accounts.user_derivative_account.clone());

            metas.push(self.accounts.underlying_vault.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeVault
    // ....................................................................

    /// Main instruction struct for InitializeVault
    pub struct InitializeVaultInstruction {
        pub accounts: InitializeVaultInstructionAccountMetas,
        pub data: InitializeVaultInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeVault instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeVaultInstructionAccountMetas {
        pub authority: AccountMeta,

        pub underlying_mint: AccountMeta,

        pub config: AccountMeta,

        pub underlying_vault: AccountMeta,

        pub derivative_mint: AccountMeta,

        pub system_program: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for InitializeVault instruction
    #[derive(Debug, Clone)]
    pub struct InitializeVaultInstructionAccounts {
        pub authority: Pubkey,

        pub underlying_mint: Pubkey,

        pub config: Pubkey,

        pub underlying_vault: Pubkey,

        pub derivative_mint: Pubkey,
    }

    impl InitializeVaultInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            underlying_mint: Pubkey,

            config: Pubkey,

            underlying_vault: Pubkey,

            derivative_mint: Pubkey,
        ) -> Self {
            Self {
                authority,

                underlying_mint,

                config,

                underlying_vault,

                derivative_mint,
            }
        }
    }

    /// Instruction data for InitializeVault
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeVaultInstructionData {
        pub id: u64,

        pub derivative_decimals: u8,
    }

    impl InitializeVaultInstructionData {
        pub fn new(id: u64, derivative_decimals: u8) -> Self {
            Self {
                id,

                derivative_decimals,
            }
        }
    }

    /// Implementation for InitializeVaultInstruction
    impl InitializeVaultInstruction {
        fn discriminator() -> [u8; 8] {
            [48u8, 191u8, 163u8, 44u8, 71u8, 129u8, 63u8, 164u8]
        }

        pub fn data(data: InitializeVaultInstructionData) -> Self {
            Self {
                accounts: InitializeVaultInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeVaultInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.underlying_mint =
                AccountMeta::new_readonly(accounts.underlying_mint, false);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.underlying_vault = AccountMeta::new(accounts.underlying_vault, false);

            self.accounts.derivative_mint = AccountMeta::new(accounts.derivative_mint, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.underlying_mint.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.underlying_vault.clone());

            metas.push(self.accounts.derivative_mint.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ProposeOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for ProposeOwnershipTransfer
    pub struct ProposeOwnershipTransferInstruction {
        pub accounts: ProposeOwnershipTransferInstructionAccountMetas,
        pub data: ProposeOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ProposeOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct ProposeOwnershipTransferInstructionAccountMetas {
        pub owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for ProposeOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct ProposeOwnershipTransferInstructionAccounts {
        pub owner: Pubkey,

        pub config: Pubkey,
    }

    impl ProposeOwnershipTransferInstructionAccounts {
        pub fn new(owner: Pubkey, config: Pubkey) -> Self {
            Self { owner, config }
        }
    }

    /// Instruction data for ProposeOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ProposeOwnershipTransferInstructionData {
        pub new_owner: Pubkey,

        pub accept_window_secs: i64,
    }

    impl ProposeOwnershipTransferInstructionData {
        pub fn new(new_owner: Pubkey, accept_window_secs: i64) -> Self {
            Self {
                new_owner,

                accept_window_secs,
            }
        }
    }

    /// Implementation for ProposeOwnershipTransferInstruction
    impl ProposeOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [5u8, 78u8, 67u8, 196u8, 223u8, 159u8, 228u8, 136u8]
        }

        pub fn data(data: ProposeOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: ProposeOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ProposeOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Redeem
    // ....................................................................

    /// Main instruction struct for Redeem
    pub struct RedeemInstruction {
        pub accounts: RedeemInstructionAccountMetas,
        pub data: RedeemInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Redeem instruction
    #[derive(Debug, Clone, Default)]
    pub struct RedeemInstructionAccountMetas {
        pub user: AccountMeta,

        pub config: AccountMeta,

        pub underlying_mint: AccountMeta,

        pub derivative_mint: AccountMeta,

        pub user_underlying_account: AccountMeta,

        pub user_derivative_account: AccountMeta,

        pub underlying_vault: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for Redeem instruction
    #[derive(Debug, Clone)]
    pub struct RedeemInstructionAccounts {
        pub user: Pubkey,

        pub config: Pubkey,

        pub underlying_mint: Pubkey,

        pub derivative_mint: Pubkey,

        pub user_underlying_account: Pubkey,

        pub user_derivative_account: Pubkey,

        pub underlying_vault: Pubkey,
    }

    impl RedeemInstructionAccounts {
        pub fn new(
            user: Pubkey,

            config: Pubkey,

            underlying_mint: Pubkey,

            derivative_mint: Pubkey,

            user_underlying_account: Pubkey,

            user_derivative_account: Pubkey,

            underlying_vault: Pubkey,
        ) -> Self {
            Self {
                user,

                config,

                underlying_mint,

                derivative_mint,

                user_underlying_account,

                user_derivative_account,

                underlying_vault,
            }
        }
    }

    /// Instruction data for Redeem
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct RedeemInstructionData {
        pub derivative_amount: u64,
    }

    impl RedeemInstructionData {
        pub fn new(derivative_amount: u64) -> Self {
            Self { derivative_amount }
        }
    }

    /// Implementation for RedeemInstruction
    impl RedeemInstruction {
        fn discriminator() -> [u8; 8] {
            [184u8, 12u8, 86u8, 149u8, 70u8, 196u8, 97u8, 225u8]
        }

        pub fn data(data: RedeemInstructionData) -> Self {
            Self {
                accounts: RedeemInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: RedeemInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.underlying_mint =
                AccountMeta::new_readonly(accounts.underlying_mint, false);

            self.accounts.derivative_mint = AccountMeta::new(accounts.derivative_mint, false);

            self.accounts.user_underlying_account =
                AccountMeta::new(accounts.user_underlying_account, false);

            self.accounts.user_derivative_account =
                AccountMeta::new(accounts.user_derivative_account, false);

            self.accounts.underlying_vault = AccountMeta::new(accounts.underlying_vault, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.underlying_mint.clone());

            metas.push(self.accounts.derivative_mint.clone());

            metas.push(self.accounts.user_underlying_account.clone());

            metas.push(self.accounts.user_derivative_account.clone());

            metas.push(self.accounts.underlying_vault.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ------------------------------------------------------------------------
    // Composite Accounts
    // ------------------------------------------------------------------------
}

// ----------------------------------------------------------------------------
// Program: merke_airdrop
// ----------------------------------------------------------------------------
pub mod merke_airdrop {
    use super::*;

    // ------------------------------------------------------------------------
    // Program ID
    // ------------------------------------------------------------------------

    /// Returns the program ID for merke_airdrop
    pub fn program_id() -> Pubkey {
        pubkey!("Dpjs4ihZc6T9Y6mBfgDcmRavoFysLRDpdW5fezbxGZ33")
    }

    // ------------------------------------------------------------------------
    // Instructions
    // ------------------------------------------------------------------------

    // ....................................................................
    // Instruction: AcceptOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for AcceptOwnershipTransfer
    pub struct AcceptOwnershipTransferInstruction {
        pub accounts: AcceptOwnershipTransferInstructionAccountMetas,
        pub data: AcceptOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AcceptOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct AcceptOwnershipTransferInstructionAccountMetas {
        pub pending_owner: AccountMeta,

        pub distributor: AccountMeta,
    }

    /// Account pubkeys for AcceptOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct AcceptOwnershipTransferInstructionAccounts {
        pub pending_owner: Pubkey,

        pub distributor: Pubkey,
    }

    impl AcceptOwnershipTransferInstructionAccounts {
        pub fn new(pending_owner: Pubkey, distributor: Pubkey) -> Self {
            Self {
                pending_owner,

                distributor,
            }
        }
    }

    /// Instruction data for AcceptOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AcceptOwnershipTransferInstructionData {}

    impl AcceptOwnershipTransferInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for AcceptOwnershipTransferInstruction
    impl AcceptOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [30u8, 187u8, 65u8, 5u8, 93u8, 131u8, 38u8, 208u8]
        }

        pub fn data(data: AcceptOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: AcceptOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AcceptOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.pending_owner = AccountMeta::new(accounts.pending_owner, true);

            self.accounts.distributor = AccountMeta::new(accounts.distributor, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.pending_owner.clone());

            metas.push(self.accounts.distributor.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CancelOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for CancelOwnershipTransfer
    pub struct CancelOwnershipTransferInstruction {
        pub accounts: CancelOwnershipTransferInstructionAccountMetas,
        pub data: CancelOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CancelOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct CancelOwnershipTransferInstructionAccountMetas {
        pub owner: AccountMeta,

        pub distributor: AccountMeta,
    }

    /// Account pubkeys for CancelOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct CancelOwnershipTransferInstructionAccounts {
        pub owner: Pubkey,

        pub distributor: Pubkey,
    }

    impl CancelOwnershipTransferInstructionAccounts {
        pub fn new(owner: Pubkey, distributor: Pubkey) -> Self {
            Self { owner, distributor }
        }
    }

    /// Instruction data for CancelOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CancelOwnershipTransferInstructionData {}

    impl CancelOwnershipTransferInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for CancelOwnershipTransferInstruction
    impl CancelOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [2u8, 184u8, 195u8, 105u8, 138u8, 142u8, 154u8, 75u8]
        }

        pub fn data(data: CancelOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: CancelOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CancelOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.distributor = AccountMeta::new(accounts.distributor, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.distributor.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Claim
    // ....................................................................

    /// Main instruction struct for Claim
    pub struct ClaimInstruction {
        pub accounts: ClaimInstructionAccountMetas,
        pub data: ClaimInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Claim instruction
    #[derive(Debug, Clone, Default)]
    pub struct ClaimInstructionAccountMetas {
        pub claimant: AccountMeta,

        pub distributor: AccountMeta,

        pub mint: AccountMeta,

        pub vault: AccountMeta,

        pub claimant_token_account: AccountMeta,

        pub claim_bitmap: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for Claim instruction
    #[derive(Debug, Clone)]
    pub struct ClaimInstructionAccounts {
        pub claimant: Pubkey,

        pub distributor: Pubkey,

        pub mint: Pubkey,

        pub vault: Pubkey,

        pub claimant_token_account: Pubkey,

        pub claim_bitmap: Pubkey,
    }

    impl ClaimInstructionAccounts {
        pub fn new(
            claimant: Pubkey,

            distributor: Pubkey,

            mint: Pubkey,

            vault: Pubkey,

            claimant_token_account: Pubkey,

            claim_bitmap: Pubkey,
        ) -> Self {
            Self {
                claimant,

                distributor,

                mint,

                vault,

                claimant_token_account,

                claim_bitmap,
            }
        }
    }

    /// Instruction data for Claim
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ClaimInstructionData {
        pub index: u32,

        pub amount: u64,

        pub proof: Vec<[u8; 32]>,
    }

    impl ClaimInstructionData {
        pub fn new(index: u32, amount: u64, proof: Vec<[u8; 32]>) -> Self {
            Self {
                index,

                amount,

                proof,
            }
        }
    }

    /// Implementation for ClaimInstruction
    impl ClaimInstruction {
        fn discriminator() -> [u8; 8] {
            [62u8, 198u8, 214u8, 193u8, 213u8, 159u8, 108u8, 210u8]
        }

        pub fn data(data: ClaimInstructionData) -> Self {
            Self {
                accounts: ClaimInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ClaimInstructionAccounts) -> Self {
            self.accounts.claimant = AccountMeta::new(accounts.claimant, true);

            self.accounts.distributor = AccountMeta::new(accounts.distributor, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.vault = AccountMeta::new(accounts.vault, false);

            self.accounts.claimant_token_account =
                AccountMeta::new(accounts.claimant_token_account, false);

            self.accounts.claim_bitmap = AccountMeta::new(accounts.claim_bitmap, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.claimant.clone());

            metas.push(self.accounts.distributor.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.vault.clone());

            metas.push(self.accounts.claimant_token_account.clone());

            metas.push(self.accounts.claim_bitmap.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeDistributor
    // ....................................................................

    /// Main instruction struct for InitializeDistributor
    pub struct InitializeDistributorInstruction {
        pub accounts: InitializeDistributorInstructionAccountMetas,
        pub data: InitializeDistributorInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeDistributor instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeDistributorInstructionAccountMetas {
        pub authority: AccountMeta,

        pub mint: AccountMeta,

        pub source_token_account: AccountMeta,

        pub distributor: AccountMeta,

        pub vault: AccountMeta,

        pub claim_bitmap: AccountMeta,

        pub system_program: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for InitializeDistributor instruction
    #[derive(Debug, Clone)]
    pub struct InitializeDistributorInstructionAccounts {
        pub authority: Pubkey,

        pub mint: Pubkey,

        pub source_token_account: Pubkey,

        pub distributor: Pubkey,

        pub vault: Pubkey,

        pub claim_bitmap: Pubkey,
    }

    impl InitializeDistributorInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            mint: Pubkey,

            source_token_account: Pubkey,

            distributor: Pubkey,

            vault: Pubkey,

            claim_bitmap: Pubkey,
        ) -> Self {
            Self {
                authority,

                mint,

                source_token_account,

                distributor,

                vault,

                claim_bitmap,
            }
        }
    }

    /// Instruction data for InitializeDistributor
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeDistributorInstructionData {
        pub id: u64,

        pub merkle_root: [u8; 32],

        pub max_claims: u32,

        pub total_funding_amount: u64,
    }

    impl InitializeDistributorInstructionData {
        pub fn new(
            id: u64,

            merkle_root: [u8; 32],

            max_claims: u32,

            total_funding_amount: u64,
        ) -> Self {
            Self {
                id,

                merkle_root,

                max_claims,

                total_funding_amount,
            }
        }
    }

    /// Implementation for InitializeDistributorInstruction
    impl InitializeDistributorInstruction {
        fn discriminator() -> [u8; 8] {
            [189u8, 128u8, 103u8, 10u8, 73u8, 227u8, 37u8, 18u8]
        }

        pub fn data(data: InitializeDistributorInstructionData) -> Self {
            Self {
                accounts: InitializeDistributorInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeDistributorInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.source_token_account =
                AccountMeta::new(accounts.source_token_account, false);

            self.accounts.distributor = AccountMeta::new(accounts.distributor, false);

            self.accounts.vault = AccountMeta::new(accounts.vault, false);

            self.accounts.claim_bitmap = AccountMeta::new(accounts.claim_bitmap, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.source_token_account.clone());

            metas.push(self.accounts.distributor.clone());

            metas.push(self.accounts.vault.clone());

            metas.push(self.accounts.claim_bitmap.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ProposeOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for ProposeOwnershipTransfer
    pub struct ProposeOwnershipTransferInstruction {
        pub accounts: ProposeOwnershipTransferInstructionAccountMetas,
        pub data: ProposeOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ProposeOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct ProposeOwnershipTransferInstructionAccountMetas {
        pub owner: AccountMeta,

        pub distributor: AccountMeta,
    }

    /// Account pubkeys for ProposeOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct ProposeOwnershipTransferInstructionAccounts {
        pub owner: Pubkey,

        pub distributor: Pubkey,
    }

    impl ProposeOwnershipTransferInstructionAccounts {
        pub fn new(owner: Pubkey, distributor: Pubkey) -> Self {
            Self { owner, distributor }
        }
    }

    /// Instruction data for ProposeOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ProposeOwnershipTransferInstructionData {
        pub new_owner: Pubkey,

        pub accept_window_secs: i64,
    }

    impl ProposeOwnershipTransferInstructionData {
        pub fn new(new_owner: Pubkey, accept_window_secs: i64) -> Self {
            Self {
                new_owner,

                accept_window_secs,
            }
        }
    }

    /// Implementation for ProposeOwnershipTransferInstruction
    impl ProposeOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [5u8, 78u8, 67u8, 196u8, 223u8, 159u8, 228u8, 136u8]
        }

        pub fn data(data: ProposeOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: ProposeOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ProposeOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.distributor = AccountMeta::new(accounts.distributor, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.distributor.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ------------------------------------------------------------------------
    // Composite Accounts
    // ------------------------------------------------------------------------
}

// ----------------------------------------------------------------------------
// Program: vesting
// ----------------------------------------------------------------------------
pub mod vesting {
    use super::*;

    // ------------------------------------------------------------------------
    // Program ID
    // ------------------------------------------------------------------------

    /// Returns the program ID for vesting
    pub fn program_id() -> Pubkey {
        pubkey!("8bSvkfYPuNqNRSSZzPD62H1dDPrACYPLLQitkYWVs75q")
    }

    // ------------------------------------------------------------------------
    // Instructions
    // ------------------------------------------------------------------------

    // ....................................................................
    // Instruction: AcceptOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for AcceptOwnershipTransfer
    pub struct AcceptOwnershipTransferInstruction {
        pub accounts: AcceptOwnershipTransferInstructionAccountMetas,
        pub data: AcceptOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AcceptOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct AcceptOwnershipTransferInstructionAccountMetas {
        pub pending_owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for AcceptOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct AcceptOwnershipTransferInstructionAccounts {
        pub pending_owner: Pubkey,

        pub config: Pubkey,
    }

    impl AcceptOwnershipTransferInstructionAccounts {
        pub fn new(pending_owner: Pubkey, config: Pubkey) -> Self {
            Self {
                pending_owner,

                config,
            }
        }
    }

    /// Instruction data for AcceptOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AcceptOwnershipTransferInstructionData {}

    impl AcceptOwnershipTransferInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for AcceptOwnershipTransferInstruction
    impl AcceptOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [30u8, 187u8, 65u8, 5u8, 93u8, 131u8, 38u8, 208u8]
        }

        pub fn data(data: AcceptOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: AcceptOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AcceptOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.pending_owner = AccountMeta::new(accounts.pending_owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.pending_owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CancelOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for CancelOwnershipTransfer
    pub struct CancelOwnershipTransferInstruction {
        pub accounts: CancelOwnershipTransferInstructionAccountMetas,
        pub data: CancelOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CancelOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct CancelOwnershipTransferInstructionAccountMetas {
        pub owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for CancelOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct CancelOwnershipTransferInstructionAccounts {
        pub owner: Pubkey,

        pub config: Pubkey,
    }

    impl CancelOwnershipTransferInstructionAccounts {
        pub fn new(owner: Pubkey, config: Pubkey) -> Self {
            Self { owner, config }
        }
    }

    /// Instruction data for CancelOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CancelOwnershipTransferInstructionData {}

    impl CancelOwnershipTransferInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for CancelOwnershipTransferInstruction
    impl CancelOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [2u8, 184u8, 195u8, 105u8, 138u8, 142u8, 154u8, 75u8]
        }

        pub fn data(data: CancelOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: CancelOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CancelOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Claim
    // ....................................................................

    /// Main instruction struct for Claim
    pub struct ClaimInstruction {
        pub accounts: ClaimInstructionAccountMetas,
        pub data: ClaimInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Claim instruction
    #[derive(Debug, Clone, Default)]
    pub struct ClaimInstructionAccountMetas {
        pub beneficiary: AccountMeta,

        pub config: AccountMeta,

        pub mint: AccountMeta,

        pub schedule: AccountMeta,

        pub beneficiary_token_account: AccountMeta,

        pub vault: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for Claim instruction
    #[derive(Debug, Clone)]
    pub struct ClaimInstructionAccounts {
        pub beneficiary: Pubkey,

        pub config: Pubkey,

        pub mint: Pubkey,

        pub schedule: Pubkey,

        pub beneficiary_token_account: Pubkey,

        pub vault: Pubkey,
    }

    impl ClaimInstructionAccounts {
        pub fn new(
            beneficiary: Pubkey,

            config: Pubkey,

            mint: Pubkey,

            schedule: Pubkey,

            beneficiary_token_account: Pubkey,

            vault: Pubkey,
        ) -> Self {
            Self {
                beneficiary,

                config,

                mint,

                schedule,

                beneficiary_token_account,

                vault,
            }
        }
    }

    /// Instruction data for Claim
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ClaimInstructionData {}

    impl ClaimInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ClaimInstruction
    impl ClaimInstruction {
        fn discriminator() -> [u8; 8] {
            [62u8, 198u8, 214u8, 193u8, 213u8, 159u8, 108u8, 210u8]
        }

        pub fn data(data: ClaimInstructionData) -> Self {
            Self {
                accounts: ClaimInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ClaimInstructionAccounts) -> Self {
            self.accounts.beneficiary = AccountMeta::new(accounts.beneficiary, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.schedule = AccountMeta::new(accounts.schedule, false);

            self.accounts.beneficiary_token_account =
                AccountMeta::new(accounts.beneficiary_token_account, false);

            self.accounts.vault = AccountMeta::new(accounts.vault, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.beneficiary.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.schedule.clone());

            metas.push(self.accounts.beneficiary_token_account.clone());

            metas.push(self.accounts.vault.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CreateSchedule
    // ....................................................................

    /// Main instruction struct for CreateSchedule
    pub struct CreateScheduleInstruction {
        pub accounts: CreateScheduleInstructionAccountMetas,
        pub data: CreateScheduleInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CreateSchedule instruction
    #[derive(Debug, Clone, Default)]
    pub struct CreateScheduleInstructionAccountMetas {
        pub authority: AccountMeta,

        pub beneficiary: AccountMeta,

        pub config: AccountMeta,

        pub mint: AccountMeta,

        pub source_token_account: AccountMeta,

        pub vault: AccountMeta,

        pub schedule: AccountMeta,

        pub system_program: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for CreateSchedule instruction
    #[derive(Debug, Clone)]
    pub struct CreateScheduleInstructionAccounts {
        pub authority: Pubkey,

        pub beneficiary: Pubkey,

        pub config: Pubkey,

        pub mint: Pubkey,

        pub source_token_account: Pubkey,

        pub vault: Pubkey,

        pub schedule: Pubkey,
    }

    impl CreateScheduleInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            beneficiary: Pubkey,

            config: Pubkey,

            mint: Pubkey,

            source_token_account: Pubkey,

            vault: Pubkey,

            schedule: Pubkey,
        ) -> Self {
            Self {
                authority,

                beneficiary,

                config,

                mint,

                source_token_account,

                vault,

                schedule,
            }
        }
    }

    /// Instruction data for CreateSchedule
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CreateScheduleInstructionData {
        pub total_amount: u64,

        pub start_ts: i64,

        pub cliff_ts: i64,

        pub end_ts: i64,
    }

    impl CreateScheduleInstructionData {
        pub fn new(total_amount: u64, start_ts: i64, cliff_ts: i64, end_ts: i64) -> Self {
            Self {
                total_amount,

                start_ts,

                cliff_ts,

                end_ts,
            }
        }
    }

    /// Implementation for CreateScheduleInstruction
    impl CreateScheduleInstruction {
        fn discriminator() -> [u8; 8] {
            [200u8, 176u8, 213u8, 214u8, 210u8, 121u8, 35u8, 225u8]
        }

        pub fn data(data: CreateScheduleInstructionData) -> Self {
            Self {
                accounts: CreateScheduleInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CreateScheduleInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.beneficiary = AccountMeta::new_readonly(accounts.beneficiary, false);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.source_token_account =
                AccountMeta::new(accounts.source_token_account, false);

            self.accounts.vault = AccountMeta::new(accounts.vault, false);

            self.accounts.schedule = AccountMeta::new(accounts.schedule, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.beneficiary.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.source_token_account.clone());

            metas.push(self.accounts.vault.clone());

            metas.push(self.accounts.schedule.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeVestingConfig
    // ....................................................................

    /// Main instruction struct for InitializeVestingConfig
    pub struct InitializeVestingConfigInstruction {
        pub accounts: InitializeVestingConfigInstructionAccountMetas,
        pub data: InitializeVestingConfigInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeVestingConfig instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeVestingConfigInstructionAccountMetas {
        pub authority: AccountMeta,

        pub mint: AccountMeta,

        pub config: AccountMeta,

        pub vault: AccountMeta,

        pub system_program: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for InitializeVestingConfig instruction
    #[derive(Debug, Clone)]
    pub struct InitializeVestingConfigInstructionAccounts {
        pub authority: Pubkey,

        pub mint: Pubkey,

        pub config: Pubkey,

        pub vault: Pubkey,
    }

    impl InitializeVestingConfigInstructionAccounts {
        pub fn new(authority: Pubkey, mint: Pubkey, config: Pubkey, vault: Pubkey) -> Self {
            Self {
                authority,

                mint,

                config,

                vault,
            }
        }
    }

    /// Instruction data for InitializeVestingConfig
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeVestingConfigInstructionData {
        pub id: u64,
    }

    impl InitializeVestingConfigInstructionData {
        pub fn new(id: u64) -> Self {
            Self { id }
        }
    }

    /// Implementation for InitializeVestingConfigInstruction
    impl InitializeVestingConfigInstruction {
        fn discriminator() -> [u8; 8] {
            [16u8, 53u8, 86u8, 253u8, 175u8, 121u8, 202u8, 87u8]
        }

        pub fn data(data: InitializeVestingConfigInstructionData) -> Self {
            Self {
                accounts: InitializeVestingConfigInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeVestingConfigInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.vault = AccountMeta::new(accounts.vault, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.vault.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Pause
    // ....................................................................

    /// Main instruction struct for Pause
    pub struct PauseInstruction {
        pub accounts: PauseInstructionAccountMetas,
        pub data: PauseInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Pause instruction
    #[derive(Debug, Clone, Default)]
    pub struct PauseInstructionAccountMetas {
        pub owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for Pause instruction
    #[derive(Debug, Clone)]
    pub struct PauseInstructionAccounts {
        pub owner: Pubkey,

        pub config: Pubkey,
    }

    impl PauseInstructionAccounts {
        pub fn new(owner: Pubkey, config: Pubkey) -> Self {
            Self { owner, config }
        }
    }

    /// Instruction data for Pause
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct PauseInstructionData {}

    impl PauseInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for PauseInstruction
    impl PauseInstruction {
        fn discriminator() -> [u8; 8] {
            [211u8, 22u8, 221u8, 251u8, 74u8, 121u8, 193u8, 47u8]
        }

        pub fn data(data: PauseInstructionData) -> Self {
            Self {
                accounts: PauseInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: PauseInstructionAccounts) -> Self {
            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ProposeOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for ProposeOwnershipTransfer
    pub struct ProposeOwnershipTransferInstruction {
        pub accounts: ProposeOwnershipTransferInstructionAccountMetas,
        pub data: ProposeOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ProposeOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct ProposeOwnershipTransferInstructionAccountMetas {
        pub owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for ProposeOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct ProposeOwnershipTransferInstructionAccounts {
        pub owner: Pubkey,

        pub config: Pubkey,
    }

    impl ProposeOwnershipTransferInstructionAccounts {
        pub fn new(owner: Pubkey, config: Pubkey) -> Self {
            Self { owner, config }
        }
    }

    /// Instruction data for ProposeOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ProposeOwnershipTransferInstructionData {
        pub new_owner: Pubkey,

        pub accept_window_secs: i64,
    }

    impl ProposeOwnershipTransferInstructionData {
        pub fn new(new_owner: Pubkey, accept_window_secs: i64) -> Self {
            Self {
                new_owner,

                accept_window_secs,
            }
        }
    }

    /// Implementation for ProposeOwnershipTransferInstruction
    impl ProposeOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [5u8, 78u8, 67u8, 196u8, 223u8, 159u8, 228u8, 136u8]
        }

        pub fn data(data: ProposeOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: ProposeOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ProposeOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Revoke
    // ....................................................................

    /// Main instruction struct for Revoke
    pub struct RevokeInstruction {
        pub accounts: RevokeInstructionAccountMetas,
        pub data: RevokeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Revoke instruction
    #[derive(Debug, Clone, Default)]
    pub struct RevokeInstructionAccountMetas {
        pub authority: AccountMeta,

        pub config: AccountMeta,

        pub mint: AccountMeta,

        pub schedule: AccountMeta,

        pub revoke_destination: AccountMeta,

        pub vault: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for Revoke instruction
    #[derive(Debug, Clone)]
    pub struct RevokeInstructionAccounts {
        pub authority: Pubkey,

        pub config: Pubkey,

        pub mint: Pubkey,

        pub schedule: Pubkey,

        pub revoke_destination: Pubkey,

        pub vault: Pubkey,
    }

    impl RevokeInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            config: Pubkey,

            mint: Pubkey,

            schedule: Pubkey,

            revoke_destination: Pubkey,

            vault: Pubkey,
        ) -> Self {
            Self {
                authority,

                config,

                mint,

                schedule,

                revoke_destination,

                vault,
            }
        }
    }

    /// Instruction data for Revoke
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct RevokeInstructionData {}

    impl RevokeInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for RevokeInstruction
    impl RevokeInstruction {
        fn discriminator() -> [u8; 8] {
            [170u8, 23u8, 31u8, 34u8, 133u8, 173u8, 93u8, 242u8]
        }

        pub fn data(data: RevokeInstructionData) -> Self {
            Self {
                accounts: RevokeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: RevokeInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.schedule = AccountMeta::new(accounts.schedule, false);

            self.accounts.revoke_destination = AccountMeta::new(accounts.revoke_destination, false);

            self.accounts.vault = AccountMeta::new(accounts.vault, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.schedule.clone());

            metas.push(self.accounts.revoke_destination.clone());

            metas.push(self.accounts.vault.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Unpause
    // ....................................................................

    /// Main instruction struct for Unpause
    pub struct UnpauseInstruction {
        pub accounts: UnpauseInstructionAccountMetas,
        pub data: UnpauseInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Unpause instruction
    #[derive(Debug, Clone, Default)]
    pub struct UnpauseInstructionAccountMetas {
        pub owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for Unpause instruction
    #[derive(Debug, Clone)]
    pub struct UnpauseInstructionAccounts {
        pub owner: Pubkey,

        pub config: Pubkey,
    }

    impl UnpauseInstructionAccounts {
        pub fn new(owner: Pubkey, config: Pubkey) -> Self {
            Self { owner, config }
        }
    }

    /// Instruction data for Unpause
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UnpauseInstructionData {}

    impl UnpauseInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for UnpauseInstruction
    impl UnpauseInstruction {
        fn discriminator() -> [u8; 8] {
            [169u8, 144u8, 4u8, 38u8, 10u8, 141u8, 188u8, 255u8]
        }

        pub fn data(data: UnpauseInstructionData) -> Self {
            Self {
                accounts: UnpauseInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UnpauseInstructionAccounts) -> Self {
            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ------------------------------------------------------------------------
    // Composite Accounts
    // ------------------------------------------------------------------------
}

// ----------------------------------------------------------------------------
// Program: converter
// ----------------------------------------------------------------------------
pub mod converter {
    use super::*;

    // ------------------------------------------------------------------------
    // Program ID
    // ------------------------------------------------------------------------

    /// Returns the program ID for converter
    pub fn program_id() -> Pubkey {
        pubkey!("87ReJzgQU1KDbRWRtGC1DTtxUZ6dE3MkcEx2a37fFySr")
    }

    // ------------------------------------------------------------------------
    // Instructions
    // ------------------------------------------------------------------------

    // ....................................................................
    // Instruction: AcceptOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for AcceptOwnershipTransfer
    pub struct AcceptOwnershipTransferInstruction {
        pub accounts: AcceptOwnershipTransferInstructionAccountMetas,
        pub data: AcceptOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AcceptOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct AcceptOwnershipTransferInstructionAccountMetas {
        pub pending_owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for AcceptOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct AcceptOwnershipTransferInstructionAccounts {
        pub pending_owner: Pubkey,

        pub config: Pubkey,
    }

    impl AcceptOwnershipTransferInstructionAccounts {
        pub fn new(pending_owner: Pubkey, config: Pubkey) -> Self {
            Self {
                pending_owner,

                config,
            }
        }
    }

    /// Instruction data for AcceptOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AcceptOwnershipTransferInstructionData {}

    impl AcceptOwnershipTransferInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for AcceptOwnershipTransferInstruction
    impl AcceptOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [30u8, 187u8, 65u8, 5u8, 93u8, 131u8, 38u8, 208u8]
        }

        pub fn data(data: AcceptOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: AcceptOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AcceptOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.pending_owner = AccountMeta::new(accounts.pending_owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.pending_owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CancelOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for CancelOwnershipTransfer
    pub struct CancelOwnershipTransferInstruction {
        pub accounts: CancelOwnershipTransferInstructionAccountMetas,
        pub data: CancelOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CancelOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct CancelOwnershipTransferInstructionAccountMetas {
        pub owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for CancelOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct CancelOwnershipTransferInstructionAccounts {
        pub owner: Pubkey,

        pub config: Pubkey,
    }

    impl CancelOwnershipTransferInstructionAccounts {
        pub fn new(owner: Pubkey, config: Pubkey) -> Self {
            Self { owner, config }
        }
    }

    /// Instruction data for CancelOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CancelOwnershipTransferInstructionData {}

    impl CancelOwnershipTransferInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for CancelOwnershipTransferInstruction
    impl CancelOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [2u8, 184u8, 195u8, 105u8, 138u8, 142u8, 154u8, 75u8]
        }

        pub fn data(data: CancelOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: CancelOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CancelOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeConverter
    // ....................................................................

    /// Main instruction struct for InitializeConverter
    pub struct InitializeConverterInstruction {
        pub accounts: InitializeConverterInstructionAccountMetas,
        pub data: InitializeConverterInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeConverter instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeConverterInstructionAccountMetas {
        pub authority: AccountMeta,

        pub from_mint: AccountMeta,

        pub to_mint: AccountMeta,

        pub config: AccountMeta,

        pub from_vault: AccountMeta,

        pub to_vault: AccountMeta,

        pub system_program: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for InitializeConverter instruction
    #[derive(Debug, Clone)]
    pub struct InitializeConverterInstructionAccounts {
        pub authority: Pubkey,

        pub from_mint: Pubkey,

        pub to_mint: Pubkey,

        pub config: Pubkey,

        pub from_vault: Pubkey,

        pub to_vault: Pubkey,
    }

    impl InitializeConverterInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            from_mint: Pubkey,

            to_mint: Pubkey,

            config: Pubkey,

            from_vault: Pubkey,

            to_vault: Pubkey,
        ) -> Self {
            Self {
                authority,

                from_mint,

                to_mint,

                config,

                from_vault,

                to_vault,
            }
        }
    }

    /// Instruction data for InitializeConverter
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeConverterInstructionData {
        pub id: u64,

        pub rate_numerator: u64,

        pub rate_denominator: u64,
    }

    impl InitializeConverterInstructionData {
        pub fn new(id: u64, rate_numerator: u64, rate_denominator: u64) -> Self {
            Self {
                id,

                rate_numerator,

                rate_denominator,
            }
        }
    }

    /// Implementation for InitializeConverterInstruction
    impl InitializeConverterInstruction {
        fn discriminator() -> [u8; 8] {
            [233u8, 203u8, 119u8, 91u8, 65u8, 243u8, 45u8, 246u8]
        }

        pub fn data(data: InitializeConverterInstructionData) -> Self {
            Self {
                accounts: InitializeConverterInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeConverterInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.from_mint = AccountMeta::new_readonly(accounts.from_mint, false);

            self.accounts.to_mint = AccountMeta::new_readonly(accounts.to_mint, false);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.from_vault = AccountMeta::new(accounts.from_vault, false);

            self.accounts.to_vault = AccountMeta::new(accounts.to_vault, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.from_mint.clone());

            metas.push(self.accounts.to_mint.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.from_vault.clone());

            metas.push(self.accounts.to_vault.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ProposeOwnershipTransfer
    // ....................................................................

    /// Main instruction struct for ProposeOwnershipTransfer
    pub struct ProposeOwnershipTransferInstruction {
        pub accounts: ProposeOwnershipTransferInstructionAccountMetas,
        pub data: ProposeOwnershipTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ProposeOwnershipTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct ProposeOwnershipTransferInstructionAccountMetas {
        pub owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for ProposeOwnershipTransfer instruction
    #[derive(Debug, Clone)]
    pub struct ProposeOwnershipTransferInstructionAccounts {
        pub owner: Pubkey,

        pub config: Pubkey,
    }

    impl ProposeOwnershipTransferInstructionAccounts {
        pub fn new(owner: Pubkey, config: Pubkey) -> Self {
            Self { owner, config }
        }
    }

    /// Instruction data for ProposeOwnershipTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ProposeOwnershipTransferInstructionData {
        pub new_owner: Pubkey,

        pub accept_window_secs: i64,
    }

    impl ProposeOwnershipTransferInstructionData {
        pub fn new(new_owner: Pubkey, accept_window_secs: i64) -> Self {
            Self {
                new_owner,

                accept_window_secs,
            }
        }
    }

    /// Implementation for ProposeOwnershipTransferInstruction
    impl ProposeOwnershipTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [5u8, 78u8, 67u8, 196u8, 223u8, 159u8, 228u8, 136u8]
        }

        pub fn data(data: ProposeOwnershipTransferInstructionData) -> Self {
            Self {
                accounts: ProposeOwnershipTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ProposeOwnershipTransferInstructionAccounts) -> Self {
            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Swap
    // ....................................................................

    /// Main instruction struct for Swap
    pub struct SwapInstruction {
        pub accounts: SwapInstructionAccountMetas,
        pub data: SwapInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Swap instruction
    #[derive(Debug, Clone, Default)]
    pub struct SwapInstructionAccountMetas {
        pub user: AccountMeta,

        pub config: AccountMeta,

        pub from_mint: AccountMeta,

        pub to_mint: AccountMeta,

        pub user_from_account: AccountMeta,

        pub user_to_account: AccountMeta,

        pub from_vault: AccountMeta,

        pub to_vault: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for Swap instruction
    #[derive(Debug, Clone)]
    pub struct SwapInstructionAccounts {
        pub user: Pubkey,

        pub config: Pubkey,

        pub from_mint: Pubkey,

        pub to_mint: Pubkey,

        pub user_from_account: Pubkey,

        pub user_to_account: Pubkey,

        pub from_vault: Pubkey,

        pub to_vault: Pubkey,
    }

    impl SwapInstructionAccounts {
        pub fn new(
            user: Pubkey,

            config: Pubkey,

            from_mint: Pubkey,

            to_mint: Pubkey,

            user_from_account: Pubkey,

            user_to_account: Pubkey,

            from_vault: Pubkey,

            to_vault: Pubkey,
        ) -> Self {
            Self {
                user,

                config,

                from_mint,

                to_mint,

                user_from_account,

                user_to_account,

                from_vault,

                to_vault,
            }
        }
    }

    /// Instruction data for Swap
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct SwapInstructionData {
        pub amount_in: u64,

        pub minimum_received: u64,
    }

    impl SwapInstructionData {
        pub fn new(amount_in: u64, minimum_received: u64) -> Self {
            Self {
                amount_in,

                minimum_received,
            }
        }
    }

    /// Implementation for SwapInstruction
    impl SwapInstruction {
        fn discriminator() -> [u8; 8] {
            [248u8, 198u8, 158u8, 145u8, 225u8, 117u8, 135u8, 200u8]
        }

        pub fn data(data: SwapInstructionData) -> Self {
            Self {
                accounts: SwapInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: SwapInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.from_mint = AccountMeta::new_readonly(accounts.from_mint, false);

            self.accounts.to_mint = AccountMeta::new_readonly(accounts.to_mint, false);

            self.accounts.user_from_account = AccountMeta::new(accounts.user_from_account, false);

            self.accounts.user_to_account = AccountMeta::new(accounts.user_to_account, false);

            self.accounts.from_vault = AccountMeta::new(accounts.from_vault, false);

            self.accounts.to_vault = AccountMeta::new(accounts.to_vault, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.from_mint.clone());

            metas.push(self.accounts.to_mint.clone());

            metas.push(self.accounts.user_from_account.clone());

            metas.push(self.accounts.user_to_account.clone());

            metas.push(self.accounts.from_vault.clone());

            metas.push(self.accounts.to_vault.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UpdateRate
    // ....................................................................

    /// Main instruction struct for UpdateRate
    pub struct UpdateRateInstruction {
        pub accounts: UpdateRateInstructionAccountMetas,
        pub data: UpdateRateInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UpdateRate instruction
    #[derive(Debug, Clone, Default)]
    pub struct UpdateRateInstructionAccountMetas {
        pub owner: AccountMeta,

        pub config: AccountMeta,
    }

    /// Account pubkeys for UpdateRate instruction
    #[derive(Debug, Clone)]
    pub struct UpdateRateInstructionAccounts {
        pub owner: Pubkey,

        pub config: Pubkey,
    }

    impl UpdateRateInstructionAccounts {
        pub fn new(owner: Pubkey, config: Pubkey) -> Self {
            Self { owner, config }
        }
    }

    /// Instruction data for UpdateRate
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UpdateRateInstructionData {
        pub rate_numerator: u64,

        pub rate_denominator: u64,
    }

    impl UpdateRateInstructionData {
        pub fn new(rate_numerator: u64, rate_denominator: u64) -> Self {
            Self {
                rate_numerator,

                rate_denominator,
            }
        }
    }

    /// Implementation for UpdateRateInstruction
    impl UpdateRateInstruction {
        fn discriminator() -> [u8; 8] {
            [24u8, 225u8, 53u8, 189u8, 72u8, 212u8, 225u8, 178u8]
        }

        pub fn data(data: UpdateRateInstructionData) -> Self {
            Self {
                accounts: UpdateRateInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UpdateRateInstructionAccounts) -> Self {
            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ------------------------------------------------------------------------
    // Composite Accounts
    // ------------------------------------------------------------------------
}

// ============================================================================
// CUSTOM TYPES
// ============================================================================

/// Custom struct: Ownership
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Ownership {
    pub owner: Pubkey,

    pub pending_owner: Pubkey,

    pub pending_expires_at: i64,
}

impl Ownership {
    pub fn new(owner: Pubkey, pending_owner: Pubkey, pending_expires_at: i64) -> Self {
        Self {
            owner,

            pending_owner,

            pending_expires_at,
        }
    }
}

/// Custom struct: VaultConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct VaultConfig {
    pub ownership: Ownership,

    pub seed_authority: Pubkey,

    pub underlying_mint: Pubkey,

    pub derivative_mint: Pubkey,

    pub underlying_vault: Pubkey,

    pub id: u64,

    pub underlying_assets: u64,

    pub derivative_decimals: u8,

    pub bump: u8,

    pub underlying_vault_bump: u8,

    pub derivative_mint_bump: u8,
}

impl VaultConfig {
    pub fn new(
        ownership: Ownership,

        seed_authority: Pubkey,

        underlying_mint: Pubkey,

        derivative_mint: Pubkey,

        underlying_vault: Pubkey,

        id: u64,

        underlying_assets: u64,

        derivative_decimals: u8,

        bump: u8,

        underlying_vault_bump: u8,

        derivative_mint_bump: u8,
    ) -> Self {
        Self {
            ownership,

            seed_authority,

            underlying_mint,

            derivative_mint,

            underlying_vault,

            id,

            underlying_assets,

            derivative_decimals,

            bump,

            underlying_vault_bump,

            derivative_mint_bump,
        }
    }
}

/// Custom struct: ClaimBitmap
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ClaimBitmap {
    pub distributor: Pubkey,

    pub max_claims: u32,

    pub bitmap: Vec<u8>,

    pub bump: u8,
}

impl ClaimBitmap {
    pub fn new(distributor: Pubkey, max_claims: u32, bitmap: Vec<u8>, bump: u8) -> Self {
        Self {
            distributor,

            max_claims,

            bitmap,

            bump,
        }
    }
}

/// Custom struct: Distributor
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Distributor {
    pub ownership: Ownership,

    pub seed_authority: Pubkey,

    pub mint: Pubkey,

    pub vault: Pubkey,

    pub merkle_root: [u8; 32],

    pub id: u64,

    pub max_claims: u32,

    pub claimed_amount: u64,

    pub bump: u8,

    pub vault_bump: u8,
}

impl Distributor {
    pub fn new(
        ownership: Ownership,

        seed_authority: Pubkey,

        mint: Pubkey,

        vault: Pubkey,

        merkle_root: [u8; 32],

        id: u64,

        max_claims: u32,

        claimed_amount: u64,

        bump: u8,

        vault_bump: u8,
    ) -> Self {
        Self {
            ownership,

            seed_authority,

            mint,

            vault,

            merkle_root,

            id,

            max_claims,

            claimed_amount,

            bump,

            vault_bump,
        }
    }
}

/// Custom struct: Ownership
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Ownership {
    pub owner: Pubkey,

    pub pending_owner: Pubkey,

    pub pending_expires_at: i64,
}

impl Ownership {
    pub fn new(owner: Pubkey, pending_owner: Pubkey, pending_expires_at: i64) -> Self {
        Self {
            owner,

            pending_owner,

            pending_expires_at,
        }
    }
}

/// Custom struct: Ownership
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Ownership {
    pub owner: Pubkey,

    pub pending_owner: Pubkey,

    pub pending_expires_at: i64,
}

impl Ownership {
    pub fn new(owner: Pubkey, pending_owner: Pubkey, pending_expires_at: i64) -> Self {
        Self {
            owner,

            pending_owner,

            pending_expires_at,
        }
    }
}

/// Custom struct: Pausable
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Pausable {
    pub paused: bool,
}

impl Pausable {
    pub fn new(paused: bool) -> Self {
        Self { paused }
    }
}

/// Custom struct: VestingConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct VestingConfig {
    pub ownership: Ownership,

    pub pausable: Pausable,

    pub seed_authority: Pubkey,

    pub mint: Pubkey,

    pub vault: Pubkey,

    pub id: u64,

    pub bump: u8,

    pub vault_bump: u8,
}

impl VestingConfig {
    pub fn new(
        ownership: Ownership,

        pausable: Pausable,

        seed_authority: Pubkey,

        mint: Pubkey,

        vault: Pubkey,

        id: u64,

        bump: u8,

        vault_bump: u8,
    ) -> Self {
        Self {
            ownership,

            pausable,

            seed_authority,

            mint,

            vault,

            id,

            bump,

            vault_bump,
        }
    }
}

/// Custom struct: VestingSchedule
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct VestingSchedule {
    pub config: Pubkey,

    pub beneficiary: Pubkey,

    pub total_amount: u64,

    pub released_amount: u64,

    pub start_ts: i64,

    pub cliff_ts: i64,

    pub end_ts: i64,

    pub revoked_at: i64,

    pub bump: u8,
}

impl VestingSchedule {
    pub fn new(
        config: Pubkey,

        beneficiary: Pubkey,

        total_amount: u64,

        released_amount: u64,

        start_ts: i64,

        cliff_ts: i64,

        end_ts: i64,

        revoked_at: i64,

        bump: u8,
    ) -> Self {
        Self {
            config,

            beneficiary,

            total_amount,

            released_amount,

            start_ts,

            cliff_ts,

            end_ts,

            revoked_at,

            bump,
        }
    }
}

/// Custom struct: ConverterConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ConverterConfig {
    pub ownership: Ownership,

    pub seed_authority: Pubkey,

    pub from_mint: Pubkey,

    pub to_mint: Pubkey,

    pub from_vault: Pubkey,

    pub to_vault: Pubkey,

    pub id: u64,

    pub rate_numerator: u64,

    pub rate_denominator: u64,

    pub bump: u8,

    pub from_vault_bump: u8,

    pub to_vault_bump: u8,
}

impl ConverterConfig {
    pub fn new(
        ownership: Ownership,

        seed_authority: Pubkey,

        from_mint: Pubkey,

        to_mint: Pubkey,

        from_vault: Pubkey,

        to_vault: Pubkey,

        id: u64,

        rate_numerator: u64,

        rate_denominator: u64,

        bump: u8,

        from_vault_bump: u8,

        to_vault_bump: u8,
    ) -> Self {
        Self {
            ownership,

            seed_authority,

            from_mint,

            to_mint,

            from_vault,

            to_vault,

            id,

            rate_numerator,

            rate_denominator,

            bump,

            from_vault_bump,

            to_vault_bump,
        }
    }
}

/// Custom struct: Ownership
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Ownership {
    pub owner: Pubkey,

    pub pending_owner: Pubkey,

    pub pending_expires_at: i64,
}

impl Ownership {
    pub fn new(owner: Pubkey, pending_owner: Pubkey, pending_expires_at: i64) -> Self {
        Self {
            owner,

            pending_owner,

            pending_expires_at,
        }
    }
}

// ============================================================================
// END OF GENERATED FILE
// ============================================================================
